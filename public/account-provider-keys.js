const keyName='atlas-apollo-key';
function mount(){const settings=document.querySelector('#settings-card');if(!settings||settings.querySelector('#apollo-key'))return;const sharing=[...settings.querySelectorAll('h2')].find(x=>x.textContent.includes('Sharing defaults'));if(!sharing)return;const section=document.createElement('div');section.innerHTML=`<h2>Apollo for optional lead discovery</h2><p>Lead Discovery can use Apollo to identify candidate companies and people before Perplexity verifies fit, timing, and current evidence. Apollo is optional. Its key remains in this browser and is sent only when you start a lead search.</p><label>Apollo API key<input id="apollo-key" type="password" autocomplete="off" placeholder="Paste an Apollo API key"></label><button id="save-apollo" type="button" class="secondary">Save Apollo key in this browser</button><p id="apollo-status" class="card">Apollo has not been configured in this browser.</p>`;while(section.firstChild)settings.insertBefore(section.firstChild,sharing);const input=settings.querySelector('#apollo-key'),status=settings.querySelector('#apollo-status');input.value=localStorage.getItem(keyName)||'';if(input.value)status.textContent='Apollo key is saved in this browser.';settings.querySelector('#save-apollo').onclick=()=>{const value=input.value.trim();if(value)localStorage.setItem(keyName,value);else localStorage.removeItem(keyName);status.textContent=value?'Apollo key saved in this browser.':'Apollo key removed.'}}
mount();new MutationObserver(mount).observe(document.documentElement,{childList:true,subtree:true});
