const shipStyles=document.createElement('link');shipStyles.rel='stylesheet';shipStyles.href='/ship-fix.css';document.head.appendChild(shipStyles);const names=['operations','baseball','legal'];const panels=[...document.querySelectorAll('[data-domain-panel]')];const buttons=[...document.querySelectorAll('[data-domain-button]')];let index=0;let timer;function show(name){index=names.indexOf(name);panels.forEach(panel=>panel.classList.toggle('active',panel.dataset.domainPanel===name));buttons.forEach(button=>button.classList.toggle('active',button.dataset.domainButton===name));}function restart(){clearInterval(timer);if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;timer=setInterval(()=>show(names[index=(index+1)%names.length]),3200);}buttons.forEach(button=>button.addEventListener('click',()=>{show(button.dataset.domainButton);restart();}));show(names[0]);restart();