const list=document.querySelector('#case-list'),detail=document.querySelector('#case-detail');const esc=v=>String(v??'—').replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':'&quot;'})[c]);async function json(url,options){const r=await fetch(url,options);if(!r.ok)throw new Error(await r.text());return r.json()}function bullets(values){return `<ul>${(values??[]).map(v=>`<li>${esc(v)}</li>`).join('')}</ul>`}function render(c){const proposal=c.latestProposal?`<section><h3>Latest AI update proposal</h3><div class="proposal"><b>${esc(c.latestProposal.checkedAt)} · confidence ${esc(c.latestProposal.confidence)}</b><p>${esc(c.latestProposal.summary)}</p>${c.latestProposal.noMaterialChange?'<p>No material change found.</p>':bullets(c.latestProposal.changes?.map(x=>`${x.field}: ${x.reason}`))}<details><summary>Raw review payload</summary><pre>${esc(JSON.stringify(c.latestProposal,null,2))}</pre></details></div></section>`:'';detail.innerHTML=`<p class="eyebrow">${esc(c.status)}</p><h2>${esc(c.title)}</h2><div class="meta"><div class="fact"><small>Court</small>${esc(c.court)}</div><div class="fact"><small>Filed</small>${esc(c.filedAt)}</div><div class="fact"><small>Index number</small>${esc(c.indexNumber||'Not publicly confirmed')}</div><div class="fact"><small>Last verified</small>${esc(c.lastVerifiedAt)}</div></div><section><h3>Core legal question</h3><div class="notice">${esc(c.coreQuestion)}</div></section><section class="columns"><div class="panel"><h3>New York position</h3>${bullets(c.positions.newYork)}</div><div class="panel"><h3>Kalshi position</h3>${bullets(c.positions.kalshi)}</div></section><section><h3>Procedural timeline</h3><div class="timeline">${c.timeline.map(x=>`<div><b>${esc(x.date)}</b>${esc(x.event)}</div>`).join('')}</div></section><section class="columns"><div class="panel"><h3>Next watch items</h3>${bullets(c.nextWatchItems)}</div><div class="panel"><h3>Uncertainties</h3>${bullets(c.analysis.uncertainties)}</div></section><section><h3>Logistics translation</h3><div class="grid">${Object.entries(c.analysis.logisticsTranslation).map(([k,v])=>`<div class="chip"><small>${esc(k)}</small>${esc(v)}</div>`).join('')}</div></section><section><h3>Sources</h3>${c.sources.map(s=>`<a class="source" href="${esc(s.url)}" target="_blank" rel="noreferrer"><b>${esc(s.title)}</b><small>${esc(s.type)} · verified ${esc(s.verifiedAt)}</small></a>`).join('')}</section>${proposal}<p class="notice">${esc(c.disclaimer)}</p>`}async function openCase(slug){const c=await json(`/api/legal/cases/${slug}`);render(c);document.querySelectorAll('.case-button').forEach(b=>b.classList.toggle('selected',b.dataset.slug===slug))}async function start(){try{const {cases}=await json('/api/legal/cases');list.innerHTML=cases.map(c=>`<button class="case-button" data-slug="${esc(c.slug)}"><b>${esc(c.shortTitle)}</b><small>${esc(c.court)} · ${esc(c.status)}</small></button>`).join('');list.addEventListener('click',e=>{const b=e.target.closest('[data-slug]');if(b)openCase(b.dataset.slug)});if(cases[0])openCase(cases[0].slug)}catch(e){list.textContent='Legal tracker unavailable.';detail.textContent=e.message}}start();