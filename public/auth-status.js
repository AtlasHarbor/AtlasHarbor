import{config,user}from'./supabase-client.js';export async function mountAuthStatus(target){const el=typeof target==='string'?document.querySelector(target):target;if(!el)return;const u=user();try{await config();el.innerHTML=`<span class="service-dot ok"></span>Supabase connected · ${u?`<a href="/account">${u.email}</a>`:'<a href="/account">Log in / sign up</a>'}`}catch{el.innerHTML='<span class="service-dot bad"></span>Supabase not configured · <a href="/account">Setup help</a>'}}mountAuthStatus('[data-auth-status]');