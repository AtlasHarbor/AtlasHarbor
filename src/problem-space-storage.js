import{supabaseSecretKey,supabaseServiceHeaders}from'./supabase-server-key.js';
import{createSupabaseUserTokenVerifier}from'./supabase-user-token.js';
import{createAccountSessionCookie}from'./account-session-cookie.js';

const JSON_HEADERS={'Content-Type':'application/json'};

async function readJson(response){
 const text=typeof response?.text==='function'?await response.text():'';
 let data={};
 try{data=text?JSON.parse(text):{}}catch{}
 if(!response?.ok){const error=new Error(data?.error_description||data?.message||data?.error||text||`Request failed (${response?.status||'unknown'}).`);error.status=response?.status||500;throw error}
 return data;
}

export function createProblemSpaceStorage({env=process.env,fetchImpl=globalThis.fetch}={}){
 const base=env.SUPABASE_URL,key=env.SUPABASE_PUBLISHABLE_KEY,secret=supabaseSecretKey(env);
 const sessionCookie=createAccountSessionCookie({env});
 const authApiKeys=[secret,key].map(value=>String(value||'').trim()).filter((value,index,values)=>value&&values.indexOf(value)===index);
 const writeQueues=new Map(),gatewaySessions=new Map();let cachedHostId=null;
 const configured=Boolean(base&&key&&secret);
 const authHeaders=(token,apiKey)=>({apikey:apiKey,Authorization:`Bearer ${token}`,...JSON_HEADERS});
 const serviceHeaders=()=>supabaseServiceHeaders(secret);
 const bearer=req=>String(req?.get?.('authorization')||'').replace(/^Bearer\s+/i,'').trim()||String(req?.get?.('x-atlas-session')||'').replace(/^Bearer\s+/i,'').trim();
 const signInError=(token,code)=>Object.assign(new Error(token?'Your account session expired or is invalid. Sign in again.':'Sign in required.'),{status:401,code:code||(token?'AUTH_SESSION_UNVERIFIED':'AUTH_TOKEN_MISSING')});
 const serial=(key,operation)=>{const prior=writeQueues.get(key)||Promise.resolve(),next=prior.then(operation,operation),settled=next.catch(()=>{});writeQueues.set(key,settled);settled.finally(()=>{if(writeQueues.get(key)===settled)writeQueues.delete(key)});return next};
 async function timedFetch(url,options={}){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),8000);try{return await fetchImpl(url,{...options,signal:controller.signal})}catch(error){if(error?.name==='AbortError')throw Object.assign(new Error('Database request timed out.'),{status:504});throw error}finally{clearTimeout(timer)}}
 const tokenVerifier=createSupabaseUserTokenVerifier({base,jwksUrl:env.SUPABASE_JWKS_URL,loadJwks:async url=>readJson(await timedFetch(url,{headers:{Accept:'application/json'}}))});
 async function ownUserResponse(token,options={}){if(!base||!token||!authApiKeys.length)return null;let rejected=null;for(const apiKey of authApiKeys){const response=await timedFetch(`${base}/auth/v1/user`,{...options,headers:{...authHeaders(token,apiKey),...(options.headers||{})}});if(response.ok)return response;if(![401,403].includes(response.status))return readJson(response);rejected=response}return rejected}
 async function remoteUserForToken(token){const response=await ownUserResponse(token);if(!response||!response.ok)return null;return response.json()}
 async function serviceAccountById(id){if(!id||!secret)return null;const response=await timedFetch(`${base}/auth/v1/admin/users/${id}`,{headers:serviceHeaders()});if(response.status===404)return null;const data=await readJson(response);return data?.user||data}
 async function gatewayProbe(token){try{const response=await timedFetch(`${base}/rest/v1/__atlas_session_verification_never_create__?select=id&limit=0`,{headers:{apikey:key,Authorization:`Bearer ${token}`,Accept:'application/json'}}),body=await response.text();let data={};try{data=body?JSON.parse(body):{}}catch{}if(response.status===404&&data?.code==='PGRST205')return{status:'accepted'};if(data?.code==='PGRST301'||/[Jj][Ww][Tt]/.test(String(data?.message||data?.details||body)))return{status:'invalid'};return{status:'unavailable',error:Object.assign(new Error(data?.message||body||`Session gateway returned ${response.status}.`),{status:response.status||503})}}catch(error){return{status:'unavailable',error}}}
 async function gatewayToken(token){const claims=tokenVerifier.validatedClaims(token);if(!claims)return{status:'unsupported'};const cached=gatewaySessions.get(token),now=Date.now();if(cached?.expiresAt>now)return{status:'verified',claims:cached.claims};if(!base||!key)return{status:'unavailable'};const checked=await gatewayProbe(token);if(checked.status!=='accepted')return checked;const parts=String(token).split('.'),signature=parts[2]||'',control=`${parts[0]}.${parts[1]}.${signature.startsWith('A')?'B':'A'}${signature.slice(1)}`,negative=await gatewayProbe(control);if(negative.status!=='invalid')return{status:'unavailable',error:Object.assign(new Error('Session gateway did not enforce the bearer signature.'),{status:503})};gatewaySessions.set(token,{claims,expiresAt:Math.min(Number(claims.exp)*1000,now+5*60*1000)});return{status:'verified',claims}}
 async function accountForSubject(id,token){let serviceError=null;try{const account=await serviceAccountById(id);if(account)return String(account.id)===id?account:null}catch(error){serviceError=error}try{const account=(await listAccounts()).find(item=>String(item?.id)===id);if(account)return account}catch(error){serviceError=serviceError||error}const remote=await remoteUserForToken(token);if(remote)return String(remote.id)===id?remote:null;if(serviceError)throw serviceError;return null}
 async function authenticateToken(token){if(!base||!token)return{current:null,verification:'missing',code:token?'AUTH_SERVER_NOT_CONFIGURED':'AUTH_TOKEN_MISSING'};const local=await tokenVerifier.verify(token);if(local.status==='verified'){const current=await accountForSubject(local.claims.sub,token);return{current,verification:'jwks',code:current?null:'AUTH_ACCOUNT_NOT_FOUND'}}const gateway=await gatewayToken(token);if(gateway.status==='verified'){const current=await accountForSubject(gateway.claims.sub,token);return{current,verification:'postgrest',code:current?null:'AUTH_ACCOUNT_NOT_FOUND'}}if(gateway.status==='invalid'||local.status==='invalid')return{current:null,verification:'rejected',code:'AUTH_TOKEN_REJECTED'};try{const current=await remoteUserForToken(token);if(current)return{current,verification:'auth-user',code:null}}catch(error){if(!gateway.error)throw error}if(gateway.error)throw gateway.error;return{current:null,verification:'auth-user',code:'AUTH_REMOTE_REJECTED'}}
 async function userForToken(token){return(await authenticateToken(token)).current}
 async function requestUser(req,{required=true}={}){const token=bearer(req),serverSession=sessionCookie.read(req);if(serverSession){const current=await serviceAccountById(serverSession.sub);if(current)return{token,current,verification:'server-session-cookie',code:null}}const result=await authenticateToken(token);if(!result.current&&required)throw signInError(token,result.code);return{token,current:result.current,verification:result.verification,code:result.code}}
 async function listAccounts(){if(!configured)throw Object.assign(new Error('Supabase server persistence is not configured.'),{status:503});const data=await readJson(await timedFetch(`${base}/auth/v1/admin/users?per_page=1000`,{headers:serviceHeaders()}));return data.users||[]}
 async function accountById(id){if(!id)return null;try{return await serviceAccountById(id)}catch{return null}}
 async function hostAccount({fallbackCurrent=null}={}){if(cachedHostId){const cached=await accountById(cachedHostId);if(cached)return cached;cachedHostId=null}const users=await listAccounts();let account=users.find(item=>item?.user_metadata?.atlas_admin?.masterUserId===item.id)||users.find(item=>item?.user_metadata?.atlas_admin)||users.find(item=>item?.user_metadata?.atlas_problem_spaces);if(!account&&fallbackCurrent)account=users.find(item=>item.id===fallbackCurrent.id)||fallbackCurrent;if(account)cachedHostId=account.id;return account||null}
 async function updateAccount(accountId,metadata){return readJson(await timedFetch(`${base}/auth/v1/admin/users/${accountId}`,{method:'PUT',headers:serviceHeaders(),body:JSON.stringify({user_metadata:metadata})}))}
 async function updateOwn(token,metadata,accountId){if(accountId&&secret)try{return await readJson(await timedFetch(`${base}/auth/v1/admin/users/${accountId}`,{method:'PUT',headers:serviceHeaders(),body:JSON.stringify({user_metadata:metadata})}))}catch(error){if(![401,403,404].includes(error.status))throw error}const response=await ownUserResponse(token,{method:'PUT',body:JSON.stringify({data:metadata})});if(!response)throw Object.assign(new Error('Supabase user authentication is not configured.'),{status:503});return readJson(response)}
 async function readHost({fallbackCurrent=null}={}){if(!configured)return null;return hostAccount({fallbackCurrent})}
 async function updateHost(updater,{fallbackCurrent=null}={}){if(!configured)throw Object.assign(new Error('Supabase server persistence is not configured.'),{status:503});return serial('host',async()=>{let account=await hostAccount({fallbackCurrent});if(!account&&fallbackCurrent)account=fallbackCurrent;if(!account)throw Object.assign(new Error('Create or sign in to an Atlas Harbor account before initializing shared Problem Space storage.'),{status:409});const metadata=await updater(structuredClone(account.user_metadata||{}),account);const saved=await updateAccount(account.id,metadata);return saved.user||saved})}
 async function readGlobal(space,{fallbackCurrent=null,defaults={}}={}){if(!configured)return structuredClone(defaults);const account=await hostAccount({fallbackCurrent});return{...structuredClone(defaults),...(account?.user_metadata?.atlas_problem_spaces?.[space]||{})}}
 async function writeGlobal(space,updater,{fallbackCurrent=null}={}){let value;const account=await updateHost(async metadata=>{const spaces={...(metadata.atlas_problem_spaces||{})},current=spaces[space]||{};value=await updater(structuredClone(current));spaces[space]=value;return{...metadata,atlas_problem_spaces:spaces}},{fallbackCurrent});return{value,account}}
 async function readUser(req,space,{defaults={}}={}){const{current}=await requestUser(req);return{user:current,value:{...structuredClone(defaults),...(current.user_metadata?.atlas_problem_spaces?.[space]||{})}}}
 async function patchUser(req,updater){const token=bearer(req),serverSession=sessionCookie.read(req),credential=serverSession?.sub||token;if(!credential)throw signInError(token);return serial(`user:${credential}`,async()=>{const{current:fresh,code}=await requestUser(req,{required:false});if(!fresh)throw signInError(token,code);const patch=await updater(structuredClone(fresh.user_metadata||{}),fresh);if(!patch||typeof patch!=='object'||Array.isArray(patch))throw Object.assign(new Error('Account metadata patch is invalid.'),{status:500});const saved=await updateOwn(token,patch,fresh.id);return{patch,user:saved.user||saved}})}
 async function writeUser(req,space,updater){const token=bearer(req),serverSession=sessionCookie.read(req),credential=serverSession?.sub||token;if(!credential)throw signInError(token);return serial(`user:${credential}`,async()=>{const{current:fresh,code}=await requestUser(req,{required:false});if(!fresh)throw signInError(token,code);const spaces={...(fresh.user_metadata?.atlas_problem_spaces||{})},value=await updater(structuredClone(spaces[space]||{}),fresh);spaces[space]=value;const metadata={...(fresh.user_metadata||{}),atlas_problem_spaces:spaces};const saved=await updateOwn(token,metadata,fresh.id);return{value,user:saved.user||saved}})}
 return{configured,userSessionVerification:'server-session-cookie-or-jwks-postgrest-auth-fallback',bearer,userForToken,requestUser,listAccounts,hostAccount,readHost,updateHost,readGlobal,writeGlobal,readUser,patchUser,writeUser,readJson};
}
