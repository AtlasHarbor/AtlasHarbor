const esc=(v)=>String(v??'—').replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':'&quot;',"'":'&#39;'})[c]);
const slug=(v)=>String(v||'player').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
const grid=(pairs)=>`<div class="decision-grid">${pairs.filter(([,v])=>v!==undefined&&v!==null&&v!=='').map(([k,v])=>`<div><span>${esc(k)}</span><strong>${esc(v)}</strong></div>`).join('')}</div>`;
const firstStat=(stats,prefix)=>Object.entries(stats||{}).find(([k,v])=>k.startsWith(prefix)&&Array.isArray(v)&&v[0])?.[1]?.[0]?.stat||{};
export const playerSlug=(p)=>slug(p.name);
export function renderBaseballPlayerPage(p,canonical){const hit=firstStat(p.stats,'hitting:season'),pit=firstStat(p.stats,'pitching:season'),field=firstStat(p.stats,'fielding:season');const bio=`${p.name} is a ${p.position?.toLowerCase()||'player'} for ${p.team}. ${p.bats?`Bats ${p.bats.toLowerCase()}. `:''}${p.throws?`Throws ${p.throws.toLowerCase()}. `:''}${p.debut?`MLB debut: ${p.debut}.`:''}`;const major=Object.keys(pit).length?[["WAR",pit.war],["ERA",pit.era],["WHIP",pit.whip],["W-L",pit.wins!=null?`${pit.wins}-${pit.losses}`:null],["IP",pit.inningsPitched],["K",pit.strikeOuts],["BB",pit.baseOnBalls],["K/9",pit.strikeoutsPer9Inn],["HR/9",pit.homeRunsPer9]]:[["WAR",hit.war],["AVG",hit.avg],["OBP",hit.obp],["SLG",hit.slg],["OPS",hit.ops],["HR",hit.homeRuns],["RBI",hit.rbi],["R",hit.runs],["SB",hit.stolenBases],["BB",hit.baseOnBalls],["K",hit.strikeOuts]];const games=(p.recentGames||[]).map(g=>{const s=g.stat||{};const summary=Object.keys(pit).length?`${s.inningsPitched??'—'} IP · ${s.strikeOuts??0} K · ${s.earnedRuns??0} ER`:`${s.hits??0}-${s.atBats??0} · ${s.homeRuns??0} HR · ${s.rbi??0} RBI · ${s.runs??0} R`;return `<tr><td>${esc(g.date)}</td><td>${g.isHome?'vs':'at'} ${esc(g.opponent)}</td><td>${esc(summary)}</td></tr>`}).join('');return `<!doctype html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(p.name)} Stats, Bio and Recent Games · Atlas Harbor</title><meta name="description" content="${esc(bio)} View major stats, WAR when available, and recent game results."><link rel="canonical" href="${esc(canonical)}"><link rel="stylesheet" href="/game-report.css"></head><body><header class="report-nav"><a class="brand" href="/"><span>AH</span>Atlas Harbor</a><nav><a href="/baseball">Baseball</a></nav></header><main><article><header class="game-hero"><p class="eyebrow">PLAYER REPORT</p><h1>${esc(p.name)}</h1><p class="dek">${esc(p.team)} · ${esc(p.position)}${p.number?` · #${esc(p.number)}`:''}</p><p>${esc(bio)}</p></header><section><h2>Major stats</h2>${grid(major)}${!major.find(([k,v])=>k==='WAR'&&v!=null)?'<p class="source-note">WAR is shown only when the upstream data source publishes it; MLB Stats API often does not.</p>':''}</section><section><h2>Bio</h2>${grid([["Age",p.age],["Height",p.height],["Weight",p.weight],["Bats",p.bats],["Throws",p.throws],["Born",[p.birthCity,p.birthCountry].filter(Boolean).join(', ')],["Debut",p.debut],["Active",p.active===true?'Yes':p.active===false?'No':null]])}</section><section><h2>Last 8 games</h2><div class="table-wrap"><table><thead><tr><th>Date</th><th>Opponent</th><th>Result</th></tr></thead><tbody>${games||'<tr><td colspan="3">No recent games available.</td></tr>'}</tbody></table></div></section></article></main><footer><a href="/baseball">← Back to baseball</a><span>Atlas Harbor Baseball Intelligence</span></footer></body></html>`}
