import express from 'express';

const MLB='https://statsapi.mlb.com/api/v1';
const season=()=>new Date().getUTCFullYear();
const dateOnly=d=>d.toISOString().slice(0,10);
const addDays=(d,n)=>new Date(d.getTime()+n*86400000);
async function get(fetchImpl,path){const response=await fetchImpl(`${MLB}${path}`,{headers:{Accept:'application/json','User-Agent':'AtlasHarbor/0.6'},signal:AbortSignal.timeout(15000)});if(!response.ok)throw new Error(`MLB Stats API responded with ${response.status}`);return response.json()}
function normalizeGame(g){return{type:'game',id:g.gamePk,name:`${g.teams?.away?.team?.name??'Away'} at ${g.teams?.home?.team?.name??'Home'}`,subtitle:`${g.officialDate??g.gameDate?.slice(0,10)} · ${g.venue?.name??'Venue TBD'}`}}
export function createBaseballSearchRouter({fetchImpl=globalThis.fetch}={}){const router=express.Router();router.get('/api/baseball/search',async(req,res)=>{const query=String(req.query.q??'').trim();if(query.length<2)return res.json({results:[]});try{const[people,teams,schedule]=await Promise.all([get(fetchImpl,`/people/search?names=${encodeURIComponent(query)}&hydrate=${encodeURIComponent('currentTeam,primaryPosition')}`),get(fetchImpl,`/teams?sportId=1&season=${season()}`),get(fetchImpl,`/schedule?sportId=1&startDate=${dateOnly(new Date())}&endDate=${dateOnly(addDays(new Date(),14))}&hydrate=team,probablePitcher,venue,weather`)]);const needle=query.toLowerCase();let rawPeople=(people.people??[]).slice(0,8);const missing=rawPeople.filter(p=>!p.currentTeam?.name).map(p=>p.id).filter(Boolean);if(missing.length){try{const hydrated=await get(fetchImpl,`/people?personIds=${missing.join(',')}&hydrate=${encodeURIComponent('currentTeam,primaryPosition')}`),byId=new Map((hydrated.people??[]).map(p=>[p.id,p]));rawPeople=rawPeople.map(p=>byId.get(p.id)||p)}catch{}}
const players=rawPeople.map(p=>({type:'player',id:p.id,name:p.fullName,subtitle:`${p.currentTeam?.name??(p.active===false?'Free agent':'Team unavailable')} · ${p.primaryPosition?.name??'Player'}`}));const matchingTeams=(teams.teams??[]).filter(t=>[t.name,t.teamName,t.clubName,t.abbreviation].some(v=>v?.toLowerCase().includes(needle))).slice(0,8).map(t=>({type:'team',id:t.id,name:t.name,subtitle:`${t.league?.name??'MLB'} · ${t.division?.name??''}`.replace(/ · $/,'')}));const games=(schedule.dates??[]).flatMap(d=>d.games??[]).filter(g=>`${g.teams?.away?.team?.name} ${g.teams?.home?.team?.name} ${g.venue?.name}`.toLowerCase().includes(needle)).map(normalizeGame).sort((a,b)=>String(a.subtitle).localeCompare(String(b.subtitle))).slice(0,10);return res.json({results:[...matchingTeams,...games,...players]})}catch(error){console.error('Baseball search failed',error);return res.status(502).json({error:'Baseball search is temporarily unavailable.'})}});return router}
